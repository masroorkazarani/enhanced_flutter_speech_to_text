name: enhanced_flutter_speech_to_text
description: A Flutter package for enhanced speech-to-text functionality.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter